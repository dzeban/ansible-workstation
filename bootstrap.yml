---

- hosts: localhost
  connection: local
  vars:
    shell_config_dir: "{{ ansible_user_dir }}/Dropbox/misc/dotfiles/shell"
    vim_config_dir: "{{ ansible_user_dir }}/Dropbox/misc/dotfiles/apps/vim"
    i3_mate_config_dir: "{{ ansible_user_dir }}/Dropbox/misc/dotfiles/wm/.i3"
    golang_version: "1.10"
  tasks:
    - include_role:
        name: rpmfusion

    - include_tasks: packages.yml
    - include_tasks: fonts.yml
    - include_tasks: keyboard.yml
    - include_tasks: terminal.yml

    - include_role: name=dropbox
    - include_role: name=golang
    - include_role: name=i3-mate
    - include_role: name=neovim

    - name: Remove urxvt because it's overriding mate-terminal as i3-sensible-terminal
      become: yes
      package:
        name: rxvt-unicode
        state: absent


      # vim + neovim + configs
      # docker, create group docker and add user to docker group
      # docker-compose
      # terraform
