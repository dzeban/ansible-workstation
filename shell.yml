---

- name: Link bash configs
  file:
    state: link
    force: yes
    src: "{{ shell_config_dir }}/{{ item }}"
    dest: "{{ ansible_user_dir }}/{{ item }}"
  with_items:
    - .bashrc
    - .bash_aliases