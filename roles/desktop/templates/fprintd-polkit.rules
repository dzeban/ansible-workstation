polkit.addRule(function(action, subject) {
    if (
        action.id == "net.reactivated.fprint.device.enroll" &&
        subject.isInGroup("{{ desktop_fingerprint_group[ansible_distribution] }}")
    ) {
        return polkit.Result.YES;
    }
});
